// Placeholder root component. Real screens (Onboarding, Dashboard, Entry,
// Deductions, Settings, Summary, Import/Export) land in later plan tasks.
export default function App(): JSX.Element {
  return (
    <div>
      <h1>My Financial Account</h1>
    </div>
  );
}
