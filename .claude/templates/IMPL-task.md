# AT-{{x.y}} — {{task title}}

- **Plan:** PLAN-{{NNNN}} · **Phase:** P{{x}} · **Commit:** `{{hash}}`
- **TC results:** #a pass (`file::test`), #b pass (`file::test`), #c manual — reason

## What was done
(2–6 bullets. Name files/classes, not code. No diffs, no test output — git has them.)

## Deviations from design
(none) — or: what & why, and whether the ANA needs a decision row.

## Notes for follow-up tasks
(Only what the *next* task genuinely needs: a new helper, a changed signature, a config key.)
