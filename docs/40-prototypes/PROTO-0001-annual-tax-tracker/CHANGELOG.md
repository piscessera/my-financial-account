---
id: PROTO-0001
type: prototype
title: Annual tax income/expense tracker — prototype
status: active
created: 2026-09-13
updated: 2026-09-13
links: [ANA-0001, REQ-0001, TC-0001]
---

# PROTO-0001: Annual tax income/expense tracker — feedback rounds

Status legend: `waiting re-review` → `accepted` / `rejected` (+reason).
On final acceptance, write-back happens: "§ UX decisions" appended to the linked ANA and
`source: prototype` UI cases added to the linked TC — note it in the last row.

Linked: ANA-0001 · REQ-0001
Prototype: `index.html` + one HTML file per screen (vanilla HTML/CSS, opens from filesystem) —
not yet built; round 1 is a style-direction pick (`design/DESIGN.md`) ahead of building the
screens.

| Round | Date | Feedback | Change applied | Status |
|-------|------|----------|----------------|--------|
| 1 | 2026-09-13 | User asked for a DESIGN.md UX/UI guideline with color/style options to choose from before building screens. | Wrote `DESIGN.md`: 3 named style directions (Ledger Jade / Slate & Amber / Warm Merit) with full light+dark token tables, type pairings, and a shared structural guideline (type scale, spacing scale, shared components). Recommended Direction A. | accepted |
| 2 | 2026-09-13 | User picked Direction B (Slate & Amber). | Recorded the decision in `design/DESIGN.md`; built the 5 clickable screens (`dashboard.html`, `entry.html`, `deductions.html`, `settings.html`, `summary.html`) plus `index.html` and a shared `styles.css`, all using Direction B tokens. Verified rendering via a local static server (light/dark). Screens include empty state (new tax year), a validation-error state, cap-exceeded state, an editable-then-locked transaction with visible history, the close-tax-year confirmation, and a closed/frozen year with a reversal-entry example. | accepted |
| 3 | 2026-09-13 | Add a "payer's tax ID" field to the income entry form, next to source/payer. | Added the field to `entry.html` (optional, 13-digit Thai tax ID). Back-propagated: REQ-0001 AC-1, ANA-0001 `transactions` table (`payer_tax_id` nullable) + decision #8, TC-0001 (case #1 updated, case #1a added for the optional/blank path). | accepted |
| 4 | 2026-09-13 | Group the tax-year ledger by month for easier monthly scanning, while keeping annual figures visible. | Reworked `entry.html`'s ledger into one table with a per-month subtotal header row (spanning all columns) plus an annual totals strip (income/WHT/count) above it, linking to `summary.html` for the full year view. Fixed a `display:flex` on `<td colspan>` rendering bug along the way (moved the flex layout to a nested `<div>`). Noted the grouping in ANA-0001's UI changes section (display-only, no new data/invariant). | accepted |
| 5 | 2026-09-13 | Also record general (non-tax) income/expense, excluded from tax calculation; asked for clarification first (reuse form vs. separate section? categories? Dashboard treatment? overlap with parked `PL-0001`) and got answers before building. | Pulled the recording half of `PL-0001` into REQ-0001 (AC-13/14/15); narrowed `PL-0001` to just the budgeting/allocation layer. ANA-0001: `transactions.tax_relevant` + `general_category` (Food/Shopping/Housing/Other), new INV-8 ("general never touches a tax figure"), `calc.computeYear()` filters `tax_relevant=true`. TC-0001: cases #34–36 added. Screens: `entry.html` gets a tax/general toggle (swaps income-section chips for category chips, hides WHT), a separate amber-bordered "รายการทั่วไป" ledger panel with its own category totals; `dashboard.html` gets a second "ทั่วไป" section below the tax section, never summed together (AC-15). | accepted |
| 6 | 2026-09-13 | Settings should allow adding new deduction categories for future yearly stimulus measures; asked whether to support all 3 cap shapes and whether one-off measures need to be hideable in later years — both confirmed. | REQ-0001 AC-16 (create category, any cap shape) / AC-17 (archive/reactivate, soft `is_active` flag, never deletes history). ANA-0001: `deduction_categories.is_active`/`is_builtin`, new solution-overview subsection, `createCategory`/`setCategoryActive` API. TC-0001: cases #37–39. `settings.html`: "+ เพิ่มหมวดหมู่ใหม่" header button + form (name, cap-shape chip picker, cap value, optional description) at `#add-category`; ledger table gets a "สถานะ" column with an archived example row and "เปิดใช้อีกครั้ง" action. | accepted |
| 7 | 2026-09-13 | Category name should be editable too, not just its cap. | REQ-0001 AC-11 widened. ANA-0001: `updateCap` widened to `updateCategory({ name?, capAmountMinor? })` (decision #11). TC-0001: case #40. `settings.html`'s SSF edit-example panel gets a name field alongside the cap field. | accepted |
| 8 | 2026-09-13 | Asked where the "Sync Google Drive" button was — clarified there isn't one by design (no Drive API; Drive syncs the folder externally) and offered options; both recommended options picked. | New REQ-0001 AC-18 (first-run folder picker) / AC-19 (read-only data-location display in Settings). ANA-0001: `dataLocation` API, decision #12. TC-0001: cases #41-42 (24/24 AC). Built `onboarding.html` (first-run folder choice + Google Drive detection note) and added a "ที่จัดเก็บข้อมูล" panel to `settings.html` (folder path, last-modified, explicit "no sync button" note); linked from `index.html`. | accepted |
| 9 | 2026-09-13 | Add CSV import/export — reversing REQ-0001's earlier "no export" call. Asked purpose (bank import vs. Excel migration vs. backup/machine-move) and safety expectations before building; user's answer (backup/machine-move only) narrowed the design considerably. | REQ-0001: AC-20 (export ledger CSV), AC-21 (export summary CSV, read-only report), AC-22 (import — this app's own format only, not a bank-statement importer), AC-23 (mandatory preview, exclude rows, closed-year rows always rejected per INV-2b), AC-24 (batch audit-log entry). ANA-0001: new solution-overview subsection, `transactions.source` (manual/import) column, `csv` API surface, decision #13. TC-0001: cases #43-47 (29/29 AC). Built `import-export.html` (export panel + import dropzone + preview table with checkboxes and per-row valid/error/closed-year status) and added it to every screen's nav + `index.html`. | accepted |
| 10 | 2026-09-13 | User confirmed the prototype overall is acceptable — no further changes. | **PROTO-0001 accepted.** Write-back done: ANA-0001 "UX decisions (from PROTO-0001)" section filled (visual direction, final screen set, ledger grouping, tax/general separation, settings category management, data-location-not-sync, CSV import safety); TC-0001 gained 2 source:prototype UI-acceptance cases (#48-49). Screens (7): `onboarding`, `dashboard`, `entry`, `deductions`, `settings`, `import-export`, `summary`, plus `index.html` and shared `styles.css`. | accepted |
