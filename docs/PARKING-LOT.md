# Parking lot

The single ledger for everything deferred: review **notes** and **ideas** (from REV files),
parked GAPs, out-of-scope items from REQs. Nothing is "noted" anywhere else.
Rule: an `open` row always has an owner (a plan id, a GAP id, or a person) — `validate-docs`
fails otherwise. Audit mode checks that every REV note/idea landed here and that closed rows
say where they went.

| PL-id | Source | Item | Owner | Status | Target / resolution |
|-------|--------|------|-------|--------|---------------------|
| PL-0001 | REQ-0001 out-of-scope (narrowed 2026-09-13, PROTO-0001 feedback) | Category-based budgeting/allocation for general (non-tax) transactions — % of income per category, spending limits, savings/investment allocation (Fund, Insurance, etc. from the existing workbook). Simple *recording* of general transactions with a fixed category (Food/Shopping/Housing/Other) was pulled into REQ-0001 (AC-13/14/15) — only the budgeting/allocation layer on top remains parked here. | orchestrator | open | Candidate for a phase-2 requirement after REQ-0001 closes |
| PL-0002 | REQ-0001 out-of-scope | Net worth / asset / investment portfolio / insurance-policy tracking (the workbook's `*-Asset` sheets) | orchestrator | open | Candidate for a phase-2 requirement after REQ-0001 closes |
| PL-0003 | IMPL-0001 AT-1.9 (PLAN-0001) | TC-0001 #26 ("no outbound network calls") is a Manual-level case describing runtime monitoring of the running app; AT-1.9 only delivered a static ESLint guard (recorded as `manual (static guard only)`). A one-time manual check (run the built app with a network monitor/proxy, confirm zero outbound traffic) is still needed before the first `/dev-release`. | orchestrator | open | Run before REQ-0001's first `/dev-release` pre-flight |
| PL-0004 | REV-0001 (PLAN-0001 P1) | TC-0001 #41 (first-run folder picker persists) verified structurally only — no display server in the sandbox. A real two-launch click-through on Windows is still owed; P1's exit criteria name #41 as `pass`. | orchestrator | open | Run at the P1→P2 handover on a real desktop, at the latest in the first `/dev-release` pre-flight |
| PL-0005 | REV-0001 (PLAN-0001 P1) | The AT-1.9 no-network lint guard covers `http`/`https`/`net` imports + bare `fetch` only; `window.fetch`, `XMLHttpRequest`, `WebSocket`, `navigator.sendBeacon` and `import { net } from 'electron'` are unrestricted. | orchestrator (PLAN-0001 P4/P5) | open | Widen `eslint.config.mjs` when real renderer screens land; re-verify at `/dev-release` |
| PL-0006 | REV-0001 (PLAN-0001 P1) | `npm run build` fails at `tsc -p tsconfig.electron.json` (`rootDir: "electron"` vs. `electron/main.ts`'s `src/main/` imports, TS6059). The fix also touches `package.json`'s `main` field and the `__dirname`-relative preload/index.html paths, or drop the redundant tsc step (vite-plugin-electron already emits the bundle `main` points at). | orchestrator (packaging / first `/dev-release`) | open | Fix before the first `/dev-release`; then add `npm run build` to the gate |
| PL-0007 | REV-0001 (PLAN-0001 P1) | Main-process IPC handlers take renderer-supplied filesystem paths unvalidated (`dataLocation:createInFolder`, `lockFile:check`), and `createInFolder` has no first-run-only guard. | orchestrator (PLAN-0001 P2, AT-2.5) | open | Fold into P2's IPC wiring task |
| PL-0008 | dev-execute (PLAN-0001 P1) | `qa-reviewer` couldn't write its own REV report: `.claude/hooks/guard-shared-files.js`'s worktree rule (`inWorktree` = any path containing `.worktrees/` or, per this session's own layout, `.claude/worktrees/...`) fires before its `docs/60-reviews/REV-*.md` read-only exemption is checked, so a reviewer dispatched inside any worktree is blocked from writing the one file it's allowed to write. Orchestrator saved REV-0001 by hand from the agent's returned report body as a workaround. | orchestrator | open | Fix the hook: check the REV-report exemption before (or independent of) the generic worktree/shared-path block for `qa-reviewer` |
| PL-0009 | REV-0001 (PLAN-0001 P1) | `transactions.amount_minor` has `CHECK (amount_minor <> 0)` but no sign convention for reversals (negative amount vs. flipped `kind`) — currently decided only at the repository-code level (AT-1.3.md note), not enforceable in the DB. | orchestrator (PLAN-0001 P2) | closed | Decided in AT-2.3: `createReversal` copies the original row's shape and negates `amountMinor` (already representable under the existing non-zero CHECK, no migration needed); `whtMinor` stays 0 on a reversal since its CHECK requires `>= 0`. See `transactions.ts`'s module header and `IMPL-0001/AT-2.3.md`. |
| PL-0010 | IMPL-0001 AT-2.5 (PLAN-0001), broadened at PLAN-0001 close (REV-0002) | Every "manual smoke test" / "manually matches PROTO-0001" done-criterion across the whole plan (AT-2.5 through AT-5.6 — IPC wiring and all nine renderer screens/components) was verified either at the handler-map level or by loading the built renderer against a stubbed `window.api` in the browser pane — no display server in this sandbox (same limitation as AT-1.1/AT-1.6/AT-1.8/PL-0004). A real `ipcRenderer.invoke`/full-app round-trip through a running Electron window, click-through of every screen with real data, has never happened. | orchestrator | open | Required before the first `/dev-release` — run the full app on a real Windows desktop and click through Onboarding → every screen → close/reopen → CSV export/import once |
| PL-0011 | REV-0002 (PLAN-0001 close) | `csv.ts`'s CSV export (`exportLedger`/`exportSummary`) writes plain UTF-8 with no BOM — Thai text in `note`/`source_payer` likely renders as mojibake if a user double-clicks the file open in Excel on Windows (the app's own re-import is unaffected, it reads with explicit `utf8`). Separately, `csvField`'s quoting doesn't neutralize a leading `=`/`+`/`-`/`@` (CSV-formula-injection into Excel/Sheets) — low severity for a single-user local app exporting only the user's own data, but cheap to close. | orchestrator | open | Prepend a `﻿` BOM and a leading-apostrophe guard for formula-trigger characters in `csv.ts`'s `csvField`/`writeFileSync` calls; bundle into a pre-release hardening pass |
| PL-0012 | REV-0003 (PLAN-0002 close) | `changeFolder`'s move mode tries to delete the old DB file while the app's live `appDatabase.ts` connection may still be open on it (`electron/main.ts`'s IPC handler only calls `openAppDatabase` for the *new* location after `changeFolder` returns). On Windows this likely makes the old-file `rmSync` fail almost every time a move is used, surfacing as `oldFolderCleanupWarning` instead of a clean delete — not a correctness bug (the warning path already exists and degrades safely, TC-0002 #12 covers it), just means the "no warning" move path may rarely occur in practice. | orchestrator | open | Consider closing/reopening the live DB connection around `changeFolder`'s move step in a follow-up task, so cleanup can usually succeed |
| PL-0013 | REV-0003 (PLAN-0002 close) | `changeFolder`'s move mode creates the target directory (`mkdirSync`) before `copyFileSync`; if the copy then fails, an empty target directory is left behind. Harmless (never touches source/config) but untidy. | orchestrator | open | Cosmetic cleanup only — low priority, bundle into a future hardening pass |
