# Daily Log — 2026-09-23

## Summary

Implemented REQ-0006 (UI refinements for Thai month abbreviations, table column alignments, Settings sub-navigation, and Deductions visual redesign) and documented REQ-0006, ANA-0006, and TC-0006 in the project index.

## Work Entries

- **REQ-0006 / ANA-0006 / TC-0006**:
  - Corrected Thai month abbreviations in `LedgerTable` to standard short forms (`ม.ค.` through `ธ.ค.`).
  - Aligned table headers and data columns across all pages (`Entry`, `RecurringChecklist`, `ManageRecurringModal`, `Summary`, `Settings`, `ImportExport`).
  - Added chip-style sub-tab navigation to `Settings.tsx` (`📈 อัตราภาษีขั้นบันได`, `🛡️ เพดานค่าลดหย่อน`, `📁 ที่จัดเก็บข้อมูล`), providing direct access and edit workflows for progressive tax brackets.
  - Redesigned `Deductions.tsx` with semantic category groupings (ครอบครัว, ประกัน, กองทุนเกษียณ 500k, ดอกเบี้ยบ้าน, บริจาค), high-contrast white card list layout, live real-time computation summary tiles, and over-cap warnings.
  - Formulated architectural design options for linking daily expense entries directly to tax deductions.
  - Verified with full test suite passes (29 test suites, 258 tests) and successful Vite/TypeScript production build.

- **DESIGN.md & Agent System Updates**:
  - Created canonical UX/UI design system reference `DESIGN.md` (and `design/DESIGN.md`) documenting Direction B ("Slate & Amber"), color tokens (Light/Dark mode), typography hierarchy (Sarabun, Chakra Petch, JetBrains Mono with tabular-nums), layout grids, component library patterns, Thai financial localization terms, and responsive standards.
  - Updated `AGENTS.md` to include `DESIGN.md` as the UX/UI design system reference for all project sessions.
  - Updated all sub-agent roles (`ux-prototyper`, `implementer`, `solution-analyst`, `qa-reviewer`, `work-planner`, `requirement-intake`, `impact-investigator`, `archivist`) to align with `AGENTS.md` and enforce `DESIGN.md` compliance.
  - Updated agent skills (`dev-standard`, `dev-prototype`, `dev-implement`, `dev-analyze`, `dev-review`, `dev-requirement`) to reference `DESIGN.md`.
- **REQ-0007 / ANA-0007 / TC-0007 / PLAN-0007 (Execution & Completion)**:
  - User approved Plan Gate for `PLAN-0007`.
  - **Phase 1 (Core & Engine)**:
    - Added migration `004-expense-deduction-linkage.ts` with `deduction_category_id` FK to `transactions` table (`schema.ts` and `migrate.ts`).
    - Implemented bracket CRUD (`addTaxBracket`, `deleteTaxBracket`, `updateBracket`, `resetTaxBracketsToDefault`, `validateBracketHierarchy`) in `src/main/repositories/settings.ts` with invariant guards (`INV-7`) and audit logging (`INV-4`).
    - Updated `transactions.ts`, `deductions.ts`, and `calc/computeYear.ts` to persist deduction tagging on transactions, aggregate linked expenses with manual deduction entries, and enforce statutory and group caps.
  - **Phase 2 (IPC & Preload)**:
    - Added IPC handlers `deductions:getSummary`, `deductions:getSourceTransactions`, `settings:addBracket`, `settings:deleteBracket`, `settings:resetBrackets` to `src/main/ipc/index.ts` and exposed them typed in `electron/preload.ts`.
  - **Phase 3 (Renderer UI Implementation & Verification)**:
    - Upgraded `Entry.tsx` and `TransactionForm.tsx` with deduction category selector for expenses and category badges in `LedgerTable.tsx`.
    - Upgraded `Deductions.tsx` with auto-aggregated amounts from expense ledger, itemized drill-down modal (`getSourceTransactions`), manual addition fields, and statutory over-cap warning badges.
    - Upgraded `Settings.tsx` with full interactive tax bracket manager (Add tier, Edit bounds and % rate, Delete tier, and Reset to 8 statutory tiers 0%–35%).
    - Ran full test suite: 29 test suites, 269 tests all passing (100%).
    - Ran TypeScript compilation and Vite production build with 0 errors.
- **REQ-0008 / ANA-0008 / TC-0008 (Custom Tax Deductible Amount per Transaction — S-path Completed)**:
  - User approved Scope Gate and Design Gate for `REQ-0008` / `ANA-0008`.
  - Added Migration `005-custom-deduction-amount.ts` and updated `schema.ts`, `migrate.ts` with `deduction_amount_minor` column on `transactions`.
  - Updated `transactions.ts`, `deductions.ts`, and `computeYear.ts` with `deductionAmountMinor`, invariant checks (`INV-8: 0 < deductionAmountMinor <= amountMinor`), and `COALESCE(deduction_amount_minor, amount_minor)` aggregation across both Income and Expense transactions.
  - Updated `TransactionForm.tsx` to enable deduction tagging for all transaction kinds, added "จำนวนเงินที่ลดหย่อนภาษีได้ (บาท)" field with auto-default sync and strict validation against over-deductions.
  - Updated `LedgerTable.tsx`, `Entry.tsx`, and `Deductions.tsx` drill-down modal to show dual amount badges (Gross + Deductible amount) and drill-down columns.
  - Verified all 29 test suites (276 tests) passing cleanly (100%) and TypeScript compilation + Vite production build with 0 errors.
- **REQ-0009 / ANA-0009 / TC-0009 (Transaction list UX enhancement & Dashboard financial insights — Completed)**:
  - Added horizontal **Thai Month Filter Chips** (`ทั้งหมด`, `ม.ค.` - `ธ.ค.`) with dynamic row counts to `Entry.tsx`.
  - Added real-time **Quick Search Bar** in `Entry.tsx` searching across notes, payers, tax IDs, and categories with a 1-click clear button.
  - Harmonized table structure, Thai short date formatting (`dd ด.ด. yy`), category badges, deduction tags, and JetBrains Mono tabular numbers across both **Tax** and **General** transactions.
  - Upgraded **Dashboard.tsx** with:
    - **Cashflow & Savings KPI**: Net Income (after WHT), Total Expenses, Net Savings Balance, Savings Rate (%), and average monthly expense rate.
    - **Tax Bracket & Headroom Advisor**: Live badge of current tax bracket tier (e.g., `0%`, `5%`), headroom to next bracket tier, and total remaining deduction headroom.
    - **Interactive Trend Chart Bridge**: Clickable monthly bars in `MonthlyTrendChart.tsx` that seamlessly navigate to `Entry.tsx` with that month pre-filtered.
  - Verified calc unit tests passing (53 tests) and TypeScript + Vite production build succeeding with 0 errors.
  - Updated all document statuses to `implemented` in `docs/INDEX.md`.
