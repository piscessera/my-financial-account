## 00:00 · dev-execute (PLAN-0001) — orchestrator
- Started continuous execution of PLAN-0001. Repo had no `main` branch/remote — created `main`
  from `master`'s HEAD (after committing the pending REQ/ANA/TC/PROTO/PLAN doc updates that were
  sitting uncommitted) and adopted it as the integration branch going forward.
- Allocated IMPL-0001 (implementation folder for PLAN-0001); added `.claude/gate.env`
  (`TEST_CMD="npm test -- --run"`, `LINT_CMD="npm run lint"`) since REQ-0001's stack (Electron/
  TS/React/Vitest/ESLint) is already decided.
- decision: cutting `dev/PLAN-0001-annual-tax-income-expense-tracker` from `main` and starting
  Phase P1 (AT-1.1, the only READY task).
- next: dispatch AT-1.1 to `implementer`.

## 00:05 · dev-implement (PLAN-0001 AT-1.1) — implementer
- Scaffolded Electron+TS+React+Vite app shell (Electron 33, Vite 5, React 18, TS 5,
  `vite-plugin-electron/simple`) + ESLint 9 flat config (`eslint.config.mjs`, not `.eslintrc.*` —
  that format is deprecated) + Prettier 3. `preload.ts` exposes an empty `contextBridge` stub.
- Verified: `tsc --noEmit`, `npm run build`, `npm run lint`, `prettier --check .` all clean.
  `npm run dev` done-criterion substituted with a ~20s process-launch check (sandbox has no
  display server) — flagged for local spot-check.
- decision: accepted the `.eslintrc.*` → `eslint.config.mjs` filename deviation (ESLint 9
  requires flat config); not a scope change.
- commits: `b9818c7` (scaffold), `893690d` (IMPL-0001/AT-1.1.md note + PLAN-0001 checkbox).
- follow-up noted (not parked — routine for a fresh scaffold): `npm audit` shows 7 devDependency
  vulnerabilities (electron/vite/esbuild toolchain); fixing needs major version bumps
  (electron@44, vite@8, vitest@5), deferred until a task actually needs them.
- next: dispatch AT-1.2 (Vitest config) to `implementer`.

## 00:10 · dev-implement (PLAN-0001 AT-1.2) — implementer
- Configured Vitest for main-process code (`vitest.config.ts` scoped to `src/main/**/*.test.ts`,
  Node environment) with one passing trivial smoke test. `npm test`/`npm run lint` green.
- commits: `ccaad19` (code), `ccbd67f` (IMPL-0001/AT-1.2.md note + PLAN-0001 checkbox).
- next: dispatch AT-1.9 (no-network lint guard) to `implementer`.

## 00:16 · dev-implement (PLAN-0001 AT-1.9) — implementer
- Added static no-network ESLint guard (flat config: forbids `http`/`https`/`net` imports incl.
  `node:`-prefixed, and the global `fetch`, with a documented allowlist mechanism). Verified
  fail-then-pass on temporary deliberate violations, then removed them; `npm test`/`npm run lint`
  clean on the real tree.
- TC-0001 #26 recorded as `manual (static guard only)` — the case is Manual-level (runtime
  network monitoring of the built app), which a static lint rule can't fully cover.
- decision: parked PL-0003 — a one-time manual runtime network-monitor check is still needed
  before REQ-0001's first `/dev-release`; the static guard satisfies P1's exit criteria for now.
- commits: `6e3b199` (code), `e3cb251` (IMPL-0001/AT-1.9.md note + TC-0001 #26 + PLAN-0001 checkbox).
- next: dispatch AT-1.3 (`[core]` Drizzle schema) to `implementer` on Opus.

## 00:35 · dev-implement (PLAN-0001 AT-1.3) — implementer (Opus)
- Drizzle schema + migration runner for all 8 tables (hand-written DDL — Drizzle can't express
  the conditional CHECKs INV-2b/INV-6 need; `schema.ts` is a typed mirror with a parity test).
  Migration runner is append-only, keyed on `PRAGMA user_version`, transactional, idempotent,
  refuses downgrade. `journal_mode=DELETE` (not WAL) deliberately — avoids Google Drive syncing
  `-wal`/`-shm` side-files out of step with the main `.db` file.
- INV-1 enforced structurally: every `*_minor`/`*_bp` column is `INTEGER` + a
  `typeof(x)='integer'` CHECK; a test scans `pragma_table_info` across all tables to prove no
  money column is REAL. 63/63 tests pass, lint/typecheck clean.
- commits: `8eaab0e` (schema+migrations+tests), `76c5c45` (docs/checkbox).
- decision: several ANA-silent column details resolved by judgment (documented in `schema.ts`'s
  header and the IMPL note) — INV-5 `currency` CHECK scoped to `transactions` only (other
  amount-bearing tables are THB-only statutory figures, no user input surface); `description`
  NOT NULL DEFAULT ''; `shared_caps.name`/`tax_brackets.sort_order` made UNIQUE.
- **open question, deferred to AT-2.3/AT-4.x:** is a reversal entry (INV-2b) represented as a
  negative `amount_minor` on the same `kind`, or a flipped `kind`? Schema currently permits
  either (`amount_minor <> 0`, not `> 0`) — the repository task must pick one and the IMPL note
  for that task should record the decision.
- next: dispatch AT-1.4 (`[core]` money helper) to `implementer`.

## 00:43 · dev-execute (PLAN-0001) — orchestrator
- AT-1.5 (seed loader) needs the actual `TAX-2025` reference data (17 deduction categories +
  caps/shared groups, 8 tax brackets) from `Puy Money Management.xlsx`, which isn't in the repo —
  only referenced by row number in ANA-0001/REQ-0001. Declined to fabricate real tax figures for
  the seed data; asked the user to paste the figures. Continued with AT-1.7 (no data dependency)
  in the meantime.

## 00:43 · dev-implement (PLAN-0001 AT-1.4) — implementer (Opus)
- Money boundary helper (`parseBahtToSatang`/`formatSatangAsBaht`): half-up rounding done on the
  digit string via `BigInt`, never float (`Math.round(x*100)` explicitly avoided/tested-against).
  Half-up is on magnitude (ties away from zero) so a reversal cancels its original exactly.
- TC-0001 #31 → pass (asserts `parseBahtToSatang('1,234.567') === 123457`, repeated for
  determinism). 83/83 tests pass, lint/tsc clean.
- decision: rejected fractional `number` args (only strings) — a JS float has already lost its
  rounding decision before this helper sees it; whole-baht numbers still accepted for seeds/tests.
- commits: `964db04` (helper+tests), `2bb15d3` (docs/checkbox).
- next: dispatch AT-1.5 (seed loader) to `implementer`.

## 00:05 · dev-implement (PLAN-0001 AT-1.7) — implementer (Opus)
- `audit_log` repository + `recordMutation()` helper (single INV-4 writer): takes the raw
  better-sqlite3 connection so it composes inside a caller's own transaction, giving the entity
  write + audit row one atomic commit (INV-2). Throws instead of returning an error, so a bad
  call rolls the caller's mutation back. Also `listEntityHistory()`, `listRecentMutations()`.
- decision: snapshots serialize with sorted keys (column order never looks like a spurious
  before→after diff); `occurredAt` overridable for the import path and deterministic tests.
- 15 new tests, full suite 98/98 pass, lint/tsc clean.
- commits: `78c81cd` (repo+tests), `d7714b0` (docs/checkbox).

## 00:10 · dev-execute (PLAN-0001) — orchestrator
- P1 is now blocked: AT-1.5 (seed loader) still needs the user's `TAX-2025` figures; AT-1.6
  (onboarding) depends on AT-1.5; AT-1.8 (lock-file guard) depends on AT-1.6. AT-2.1's
  dependencies (AT-1.3, AT-1.7) are satisfied, but per `/dev-execute` rules a plan's tasks run
  strictly in phase order — not jumping ahead to P2 while P1 has an open, blocked task.
- Pausing this stream at AT-1.5 pending the user's reply; state left `executing` (resumable, not
  a hard escalation — waiting on a single user input, not a failure).
- next: resume with AT-1.5 once `TAX-2025` figures are supplied.
